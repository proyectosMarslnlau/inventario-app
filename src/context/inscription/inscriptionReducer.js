//Importamos los TYPES
export const ENVIAR_INFORMACION_USUARIO = 'ENVIAR_INFORMACION_USUARIO';
import {} from '../../type/index';

export default (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
